import DBreadcrumbItem from '../breadcrumb/src/breadcrumb-item';

/* istanbul ignore next */
DBreadcrumbItem.install = function(Vue) {
  Vue.component(DBreadcrumbItem.name, DBreadcrumbItem);
};

export default DBreadcrumbItem;
