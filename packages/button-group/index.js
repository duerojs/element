import DButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
DButtonGroup.install = function(Vue) {
  Vue.component(DButtonGroup.name, DButtonGroup);
};

export default DButtonGroup;
