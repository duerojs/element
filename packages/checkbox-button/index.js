import DCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
DCheckboxButton.install = function(Vue) {
  Vue.component(DCheckboxButton.name, DCheckboxButton);
};

export default DCheckboxButton;
