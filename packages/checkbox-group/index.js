import DCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
DCheckboxGroup.install = function(Vue) {
  Vue.component(DCheckboxGroup.name, DCheckboxGroup);
};

export default DCheckboxGroup;
